export interface University {
  domains: string[];
  alpha_two_code: string;
  country: string;
  web_pages: string[];
  name: string;
  state_province: string;
}

export interface Country {
  name: string;
}
